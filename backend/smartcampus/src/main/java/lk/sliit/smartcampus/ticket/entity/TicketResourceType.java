package lk.sliit.smartcampus.ticket.entity;
//temp file since resource code is not merged yet, will be remapped and removed once resource code is merged
public enum TicketResourceType {
    LECTURE_HALL,
    LAB,
    MEETING_ROOM,
    EQUIPMENT
}